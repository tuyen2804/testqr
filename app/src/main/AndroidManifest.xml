<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"
        tools:ignore="PhotoAndVideoPolicy" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".resultscan.ResultMultiQrHostActivity"
            android:exported="false"
            android:label="@string/title_activity_result_multi_qr_host"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name=".resultscan.ResultMultiQrDetailActivity"
            android:exported="false"
            android:label="@string/title_activity_result_multi_qr_detail"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name=".resultscan.ResultMultiQrActivity"
            android:exported="false"
            android:label="@string/title_activity_result_multi_qr"
            android:theme="@style/Theme.MyApplication" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".resultscan.ResultScanActivity"
            android:exported="false"
            android:label="@string/title_activity_result_scan"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name=".resultscan.ResultDeleteMultiQrActivity"
            android:exported="false"
            android:label="@string/title_activity_result_scan"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>